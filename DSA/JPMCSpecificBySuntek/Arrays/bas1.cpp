#include<bits/stdc++.h>
using namespace std;

int main(){

    vector<int> arr = {6,5,9,8,9};
    // find
    /*
    1. maximum of the array
    2. second maxumum
    3. second max which is not equal to max
    */
    if(arr.size() < 2){
        if(arr.size()==0) {
            cout << "Array is empty" <<endl;
            return 0;
        }
        else if(arr.size()==1){
            cout << 
        }
    }
    // maximum
    
    return 0;
}